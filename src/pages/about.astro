---
import BaseLayout from '../layouts/BaseLayout.astro';
import { resolvePath } from '../lib/paths';

const photos = [
  {
    src: '/images/wedding-charlie.jpg',
    alt: 'Anna and Charlie at a wedding',
    span: 'md:col-span-2 md:row-span-2 object-[50%_35%]',
  },
  {
    src: '/images/ULArocket-photo.jpg',
    alt: 'Anna near a ULA rocket',
    span: 'rocket-card md:col-span-1 md:row-span-2 object-[50%_40%]',
  },
  { src: '/images/backpacking.jpg', alt: 'Anna backpacking outdoors', span: 'md:col-span-1' },
  { src: '/images/faith.jpg', alt: 'Anna at church or a faith event', span: 'md:col-span-1 object-[50%_38%]' },
  { src: '/images/surf-anna.jpg', alt: 'Anna surfing', span: 'md:col-span-1' },
  { src: '/images/family.jpg', alt: 'Anna with family', span: 'md:col-span-1' },
];
---

<BaseLayout title="About | Anna Ford-Richards" description="Background, research interests, and project motivation.">
  <section class="space-y-8">
    <div class="space-y-4">
      <h1 class="text-4xl font-bold">About Me</h1>
      <p class="max-w-3xl text-slate-300">
        I am a Space Force fellow focused on cislunar operations, orbital analysis, and how agents can support research and engineering workflows. My time as a Space Force engineer at Vandenberg gave me firsthand experience working with launch companies, helping bridge technical requirements, operational coordination, and mission execution.
      </p>
      <p class="max-w-3xl text-slate-300">
        I chose this project with my advisor because I see a real gap between what agents can say about rendezvous and what operators can actually use. Outside of the classroom, I love backpacking, hiking, and just about any outdoor sport, and I bring the same curiosity and energy to research that I do to time outdoors. Faith and family are deeply important to me, and my husband and I were active in a wonderful church community in California where we helped with kids' church. I am also, proudly, a chocolate lover.
      </p>
      <p class="max-w-3xl text-orbital/90">
        Hover over the rocket photo to see where ideas launch.
      </p>
    </div>

    <div class="grid gap-4 md:grid-cols-3 md:auto-rows-[180px]">
      {photos.map((photo) => (
        <figure class={`photo-card group relative overflow-hidden rounded-2xl border border-white/10 bg-white/5 shadow-glow ${photo.span}`}>
          <img src={resolvePath(photo.src)} alt={photo.alt} class={`h-full w-full object-cover transition duration-500 group-hover:scale-105 group-hover:brightness-110 ${photo.src === '/images/wedding-charlie.jpg' ? 'object-[50%_35%]' : ''} ${photo.src === '/images/faith.jpg' ? 'object-[50%_38%]' : ''} ${photo.src === '/images/ULArocket-photo.jpg' ? 'object-[50%_40%]' : ''}`} />
          {photo.src === '/images/ULArocket-photo.jpg' && (
            <>
              <div class="rocket-whoosh pointer-events-none fixed inset-0 z-30 opacity-0"></div>
              <div class="rocket pointer-events-none fixed left-0 top-0 z-50 text-7xl opacity-0">🚀</div>
              <div class="rocket-end-glow pointer-events-none fixed z-40 opacity-0"></div>
              <div class="pointer-events-none absolute left-3 top-3 rounded-full bg-black/50 px-3 py-2 text-xs text-white/85 opacity-0 backdrop-blur transition duration-300 group-hover:opacity-100">
                Hover here — watch the rocket launch across the whole page.
              </div>
            </>
          )}
        </figure>
      ))}
    </div>
  </section>

  <style>
    .rocket-whoosh {
      background:
        radial-gradient(circle at 20% 80%, rgba(121, 242, 255, 0.16), transparent 26%),
        radial-gradient(circle at 80% 20%, rgba(255, 179, 71, 0.14), transparent 20%),
        linear-gradient(180deg, rgba(5, 8, 22, 0.0), rgba(121, 242, 255, 0.08), rgba(5, 8, 22, 0.0));
      mix-blend-mode: screen;
      pointer-events: none;
    }

    .rocket {
      left: -10vw;
      top: 76vh;
      filter: drop-shadow(0 0 16px rgba(255, 179, 71, 0.9)) drop-shadow(0 0 30px rgba(121, 242, 255, 0.7));
      transform-origin: center;
      text-shadow: 0 0 18px rgba(255, 179, 71, 0.65);
      pointer-events: none;
    }

    .rocket-end-glow {
      width: 160px;
      height: 160px;
      right: -20px;
      top: -20px;
      border-radius: 9999px;
      background: radial-gradient(circle, rgba(255, 179, 71, 0.55), rgba(121, 242, 255, 0.18), transparent 70%);
      filter: blur(8px);
      pointer-events: none;
    }

    .photo-card:hover .rocket-whoosh {
      animation: pageWhoosh 2.4s ease-out forwards;
    }

    .photo-card:hover .rocket {
      animation: rocketAcross 2.4s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    }

    .photo-card:hover .rocket-end-glow {
      animation: endGlow 2.4s ease-out forwards;
    }

    @keyframes pageWhoosh {
      0% { opacity: 0; }
      12% { opacity: 1; }
      45% { opacity: 0.85; }
      100% { opacity: 0; }
    }

    @keyframes rocketAcross {
      0% { opacity: 0; transform: translate(0, 0) scale(1.1) rotate(-10deg); }
      8% { opacity: 1; }
      70% { opacity: 1; }
      100% { opacity: 1; transform: translate(42vw, -104vh) scale(2.2) rotate(18deg); }
    }

    @keyframes endGlow {
      0% { opacity: 0; transform: scale(0.7); }
      20% { opacity: 1; }
      100% { opacity: 0; transform: scale(1.4); }
    }
  </style>
</BaseLayout>
